# train_sentiment_model.py - fine-tune DistilBERT (requires transformers & torch, GPU recommended)
import argparse, pandas as pd
from sklearn.model_selection import train_test_split

print('This script trains a DistilBERT model. See README for execution steps.')
